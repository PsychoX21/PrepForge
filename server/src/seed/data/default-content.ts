/**
 * Barrel export for all default content tracks.
 */
import { QUANT_TRACK } from './quant-track';
import { SWE_TRACK } from './swe-track';
import { RESUME_TRACK } from './resume-track';

export const DEFAULT_TRACKS = [QUANT_TRACK, SWE_TRACK, RESUME_TRACK];
